{
  "name": "deploy-button-sandbox",
  "version": "1.0.0",
  "private": true,
  "description": "A disposable, secret-free deployment target for Deploy Button.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "npm run build && node scripts/check.mjs",
    "preview": "node scripts/preview.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}

